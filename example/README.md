# React Native File Logger Example

## Installation

Build the lib

```sh
cd ..
npm i
npm run build
```

Build the example app

```sh
cd example
npm i

npm run android
# or
(cd ios && pod install)
npm run ios
```
